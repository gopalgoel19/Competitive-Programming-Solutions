#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false); 
	int n;
	while(scanf("%d",&n)!=EOF){
		vector<int> v(n);
		for(int i=0;i<n;i++) cin>>v[i];
		
	}
}